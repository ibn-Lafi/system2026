/** @type {import('next').NextConfig} */
const nextConfig = {
  transpilePackages: ["@system2026/ui", "@system2026/utils", "@system2026/database", "@system2026/validation"],
};

export default nextConfig;
